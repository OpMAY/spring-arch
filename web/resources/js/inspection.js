/**
 * FindRegex,
 * regex 형태를 찾아주는 함수
 *
 * @param {string} type 찾을 regex 타입
 * */
function findRegex(type) {
    switch (type) {

        default:
            throw new Error(`${type} is not define`);
    }
}

